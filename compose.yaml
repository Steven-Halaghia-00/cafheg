services:
  postgres:
    image: 'postgres:18.1'
    environment:
      - 'POSTGRES_DB=cafheg'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=cafheg'
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data: